
<h1>Ingrese la contraseña para acceder al enlace acortado</h1>
  <%= form_tag(authenticate_slug_path, method: :post) do %>
    <label for="password">Contraseña:</label>
    <%= password_field_tag :password %>
    <%= submit_tag 'Acceder' %>
  <% end %>
